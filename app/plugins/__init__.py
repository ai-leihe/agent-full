"""平台内置插件包。

目录结构按「槽位」组织：

* ``loaders``     —— 原始文件 → 纯文本
* ``sql_loaders`` —— 关系型数据库 → 纯文本（数据源起点）
* ``cleaners``    —— 文本清洗
* ``splitters``   —— 文本切片
* ``embedders``   —— 向量化
* ``stores``      —— 向量入库与检索

新增插件只需在本包内新增模块（或用 ``ext_plugins/`` 目录热插拔）。
"""
